diff --git a/web/src/router/index.ts b/web/src/router/index.ts
index 37708c6..ad2d03e 100644
--- a/web/src/router/index.ts
+++ b/web/src/router/index.ts
@@ -29,7 +29,13 @@ const storesThemeConfig = useThemeConfig(pinia);
 const {themeConfig} = storeToRefs(storesThemeConfig);
 const {isRequestRoutes} = themeConfig.value;
 import {useUserInfo} from "/@/stores/userInfo";
-const { userInfos } = storeToRefs(useUserInfo());
+
+/**
+ * 防止多次重复初始化路由的标记
+ * 避免在路由守卫中并发调用 initBackEndControlRoutes 导致 dynamicRoutes 被重复设置
+ */
+let isRoutesInitializing = false;
+let routesInitPromise: Promise<any> | null = null;
 
 /**
  * 创建一个可以被 Vue 应用程序使用的路由实例
@@ -117,6 +123,8 @@ router.beforeEach(async (to, from, next) => {
     NProgress.configure({showSpinner: false});
     if (to.meta.title) NProgress.start();
     const token = Session.get('token');
+    // 动态获取 userInfos，避免模块加载时 store 还未从 sessionStorage 恢复
+    const { userInfos } = storeToRefs(useUserInfo());
     if (to.path === '/login' && !token) {
         next();
         NProgress.done();
@@ -139,10 +147,21 @@ router.beforeEach(async (to, from, next) => {
             if (routesList.value.length === 0) {
                 if (isRequestRoutes) {
                     // 后端控制路由：路由数据初始化，防止刷新时丢失
-                    await initBackEndControlRoutes();
+                    // 防止多次重复初始化
+                    if (isRoutesInitializing && routesInitPromise) {
+                        await routesInitPromise;
+                    } else {
+                        isRoutesInitializing = true;
+                        routesInitPromise = initBackEndControlRoutes();
+                        try {
+                            await routesInitPromise;
+                        } finally {
+                            isRoutesInitializing = false;
+                            routesInitPromise = null;
+                        }
+                    }
                     // 解决刷新时，一直跳 404 页面问题，关联问题 No match found for location with path 'xxx'
                     // to.query 防止页面刷新时，普通路由带参数时，参数丢失。动态路由（xxx/:id/:name"）isDynamic 无需处理
-
                     next({ path: to.path, query: to.query });
                 } else {
                     // https://gitee.com/lyt-top/vue-next-admin/issues/I5F1HP
diff --git a/web/src/utils/service.ts b/web/src/utils/service.ts
index 0080f78..ae8d478 100644
--- a/web/src/utils/service.ts
+++ b/web/src/utils/service.ts
@@ -81,10 +81,13 @@ function createService() {
 						Session.clear();
 						dataAxios.msg = '登录认证失败，请重新登录';
 						ElMessageBox.alert(dataAxios.msg, '提示', {
-							confirmButtonText: 'OK',
-							callback: (action: Action) => {
-								// window.location.reload();
+							confirmButtonText: '确定',
+							callback: () => {
+								window.location.href = window.location.origin + '/#/login';
 							},
+						}).catch(() => {
+							// 用户关闭弹窗时也跳转
+							window.location.href = window.location.origin + '/#/login';
 						});
 						errorCreate(`${dataAxios.msg}: ${response.config.url}`);
 						break;
@@ -117,10 +120,12 @@ function createService() {
 					Session.clear();
 					error.message = '登录授权过期，请重新登录';
 					ElMessageBox.alert(error.message, '提示', {
-						confirmButtonText: 'OK',
-						callback: (action: Action) => {
-							window.location.reload();
+						confirmButtonText: '确定',
+						callback: () => {
+							window.location.href = window.location.origin + '/#/login';
 						},
+					}).catch(() => {
+						window.location.href = window.location.origin + '/#/login';
 					});
 					break;
 				case 403:
diff --git a/web/src/views/system/login/component/account.vue b/web/src/views/system/login/component/account.vue
index f6d224b..a9a0a54 100644
--- a/web/src/views/system/login/component/account.vue
+++ b/web/src/views/system/login/component/account.vue
@@ -162,7 +162,8 @@ export default defineComponent({
 							} else {
 								// 模拟后端控制路由，isRequestRoutes 为 true，则开启后端控制路由
 								// 添加完动态路由，再进行 router 跳转，否则可能报错 No match found for location with path "/"
-								initBackEndControlRoutes();
+								// 必须 await 等动态路由初始化完成后再跳转
+								await initBackEndControlRoutes();
 								// 执行完 initBackEndControlRoutes，再执行 signInSuccess
 								loginSuccess();
 							}
diff --git a/web/src/views/system/login/component/changePwd.vue b/web/src/views/system/login/component/changePwd.vue
index 2280dce..9f433d4 100644
--- a/web/src/views/system/login/component/changePwd.vue
+++ b/web/src/views/system/login/component/changePwd.vue
@@ -168,7 +168,8 @@ export default defineComponent({
 							} else {
 								// 模拟后端控制路由，isRequestRoutes 为 true，则开启后端控制路由
 								// 添加完动态路由，再进行 router 跳转，否则可能报错 No match found for location with path "/"
-								initBackEndControlRoutes();
+								// 必须 await 等动态路由初始化完成后再跳转
+								await initBackEndControlRoutes();
 								// 执行完 initBackEndControlRoutes，再执行 signInSuccess
 								loginSuccess();
 							}
