diff --git a/web/.env b/web/.env
index a77f485..8bf4624 100644
--- a/web/.env
+++ b/web/.env
@@ -1,8 +1,17 @@
-# port 端口号
+# 默认环境（开发环境）
+ENV = 'development'
 VITE_PORT = 8080
 VITE_API_URL = 'http://127.0.0.1:8000'
+VITE_WS_URL = 'ws://127.0.0.1:8000'
+
 # open 运行 npm run dev 时自动打开浏览器
 VITE_OPEN = false
 
 # public path 配置线上环境路径（打包）、本地通过 http-server 访问时，请置空即可
 VITE_PUBLIC_PATH = /
+
+# 是否启用按钮权限
+VITE_PM_ENABLED = true
+
+# 构建输出目录
+VITE_DIST_PATH = dist
diff --git a/web/.env.development b/web/.env.development
index fc47d19..c7ba219 100644
--- a/web/.env.development
+++ b/web/.env.development
@@ -1,10 +1,19 @@
-# 本地环境
+# 开发环境
 ENV = 'development'
 VITE_PORT = 8080
 VITE_API_URL = 'http://127.0.0.1:8000'
+
+# 开发环境 WebSocket 地址
+VITE_WS_URL = 'ws://127.0.0.1:8000'
+
 # open 运行 npm run dev 时自动打开浏览器
 VITE_OPEN = false
 
-
 # 是否启用按钮权限
 VITE_PM_ENABLED = true
+
+# Public Path 配置
+VITE_PUBLIC_PATH = /
+
+# 构建输出目录
+VITE_DIST_PATH = dist
diff --git a/web/.env.production b/web/.env.production
index 998aa5e..ca2b092 100644
--- a/web/.env.production
+++ b/web/.env.production
@@ -1,8 +1,17 @@
-# 线上环境
+# 生产环境
 ENV = 'production'
 
-# 线上环境接口地址
-VITE_API_URL = '/api' # docker-compose部署不需要修改，nginx容器自动代理了这个地址
+# 生产环境接口地址
+VITE_API_URL = '/api'
+
+# 生产环境 WebSocket 地址
+VITE_WS_URL = 'wss://${HOST}'
 
 # 是否启用按钮权限
 VITE_PM_ENABLED = true
+
+# Public Path 配置
+VITE_PUBLIC_PATH = /
+
+# 构建输出目录
+VITE_DIST_PATH = dist
diff --git a/web/package.json b/web/package.json
index 113381b..9bb746a 100644
--- a/web/package.json
+++ b/web/package.json
@@ -5,8 +5,12 @@
 	"license": "MIT",
 	"scripts": {
 		"dev": "vite --force",
+		"dev:test": "vite --force --mode test",
+		"dev:prod": "vite --force --mode production",
 		"build:dev": "vite build --mode development",
+		"build:test": "vite build --mode test",
 		"build": "vite build",
+		"build:prod": "vite build --mode production",
 		"build:local": "vite build --mode local_prod",
 		"lint-fix": "eslint --fix --ext .js --ext .jsx --ext .vue src/",
 		"build:flowH5": "vite build --config flowH5.config.ts"
