# 测试环境
ENV = 'test'

# 测试环境接口地址
VITE_API_URL = 'http://127.0.0.1:8000'

# 测试环境 WebSocket 地址
VITE_WS_URL = 'ws://127.0.0.1:8000'

# 测试环境端口号
VITE_PORT = 8080

# 是否启用按钮权限
VITE_PM_ENABLED = true

# 构建输出目录
VITE_DIST_PATH = dist

# Public Path 配置
VITE_PUBLIC_PATH = /

# open 运行时自动打开浏览器
VITE_OPEN = false
