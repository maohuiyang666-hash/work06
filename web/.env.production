# 生产环境
ENV = 'production'

# 生产环境接口地址
VITE_API_URL = '/api'

# 生产环境 WebSocket 地址
VITE_WS_URL = 'wss://${HOST}'

# 是否启用按钮权限
VITE_PM_ENABLED = true

# Public Path 配置
VITE_PUBLIC_PATH = /

# 构建输出目录
VITE_DIST_PATH = dist
